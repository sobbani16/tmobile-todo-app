import { SortByPriorityPipe } from './sort-by-priority.pipe';

describe('SortByPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
